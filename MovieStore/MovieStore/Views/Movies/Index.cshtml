@model List<MovieStore.DTOs.ViewModels.MovieViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>


@for (int i = 0; i < Model.Count(); i++)
{

    if (i%2 == 0)
    {
        @:<div class="row">
    }

    if (i%2 == 0 && i != 0)
    {
        @:</div>
    }

    @Html.Partial("_MoviePartial", Model[i])

}
